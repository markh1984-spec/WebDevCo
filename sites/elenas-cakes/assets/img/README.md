# Cake photos

The images here are placeholders so the site looks finished before Elena's
real photos arrive. Replace them with the real thing.

- `cake-01.jpg` — main hero photo (portrait, roughly 4:5)
- `cake-02.jpg` — small overlapping hero photo (square)
- `elena.jpg`   — About section portrait (roughly 4:5)
- `cake-03.jpg` … `cake-08.jpg` — gallery photos (square crops work best)

After adding photos, update the list in `../js/gallery.js`. Filenames in that
list that don't exist are skipped, so nothing breaks mid-swap.

Aim for roughly 1200–1600px on the long edge and save as JPEG around quality
80 — big enough to look sharp, small enough to load fast on a phone.
